{# Ce template n'hérite de personne, tout comme le template inclu #}

<ul>
    {% for article in liste_articles %}
        <li><a href="{{ path('blog_voir', {'id': article.id}) }}">{{ article.titre }}</a></li>
    {% endfor %}
</ul>